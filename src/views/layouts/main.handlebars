<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700;800;900&family=Open+Sans:wght@400;600;700&display=swap"
        rel="stylesheet">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="css/header.css">
    <link rel="stylesheet" href="css/normalize.css">
</head>

<body>
    <header class="header">
        <nav class="header__nav">
            <ul class="header__nav-ul">
                <div class="header__nav-ul-views">
                    <li><a href="/">Inicio</a></li>

                    {{#if (isAdmin session.user.role)}}
                    <li class="products"><a href="/products">Productos</a></li>
                    {{/if}}
                </div>
                <div class="header__nav-ul-session">
                    {{#if session.user.name}}
                    <li class="cart"><a href="/cart"><img src="/img/cart.png" alt="Carrito"></a></li>
                    <li class="logout"><a href="/login">Cerrar sesión</a></li>
                    {{else}}
                    <li><a href="/login">Iniciar sesión</a></li>
                    <li><a href="/register">Registrarse</a></li>
                </div>
                {{/if}}
            </ul>
        </nav>
    </header>
    {{{body}}}

    <script src="js/logout.js"></script>
</body>

</html>